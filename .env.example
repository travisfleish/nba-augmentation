# Copy to .env.local and fill in from Supabase → Project Settings → API.
# When these are set the app reads/writes live Supabase data.
# When they are blank the app runs in DEMO mode off the bundled seed (src/seed.json).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
